[Unit]
Description=Update dotfiles git repository

[Service]
ExecStart=/usr/bin/bash -c " \
    set -x ; \
    cd ~/.dotfiles && \
    git pull --rebase || \
    git rebase --abort"

[Install]
WantedBy=multi-user.target
